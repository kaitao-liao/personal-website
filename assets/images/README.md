# Image placeholders

- `TODO:` Add a professional portrait as `portrait.jpg`, then update the placeholder in `index.qmd`.
- `TODO:` Add a Flight Connection Probability Simulator screenshot as `flight-simulator.jpg`, then update the placeholder in `index.qmd`.
- `TODO:` Replace `favicon-placeholder.svg` with the final favicon and update `_quarto.yml` if its filename changes.

